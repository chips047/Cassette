name: Build Cassette Cross-Platform

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason'
        required: false
        default: 'Manual Building'

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        include:
          - os: ubuntu-22.04
            artifact_name: linux-x64
            sep: ":"

          - os: windows-latest
            artifact_name: windows-x64

          - os: macos-15-intel
            artifact_name: macos-intel

          - os: macos-latest
            artifact_name: macos-silicon

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install System Dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgl1-mesa-dev

      - name: Install Build Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install wheel setuptools numpy pythran

      - name: Clone and build aubio
        run: |
          git clone https://github.com/aubio/aubio.git
          cd aubio
          python setup.py bdist_wheel
          cd ..

      - name: Switch to PyQt5 for Windows build
        if: runner.os == 'Windows'
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          root = Path('.')
          skip_dirs = {'.git', '.venv', 'venv', '__pycache__', 'build', 'dist'}
          skip_files = {'System/Interface/Animation/LoomEngine.py'}
          suffixes = {'.py', '.txt'}

          for path in root.rglob('*'):
              if any(part in skip_dirs for part in path.parts):
                  continue

              if not path.is_file() or path.suffix.lower() not in suffixes:
                  continue

              relative_path = path.relative_to(root).as_posix()
              if relative_path in skip_files:
                  continue

              text = path.read_text(encoding='utf-8')
              updated = text.replace('PyQt6', 'PyQt5').replace('pyqt6', 'pyqt5')
              if updated != text:
                  path.write_text(updated, encoding='utf-8')
                  print(relative_path)
          PY

      - name: Setup Venv and Install Project
        shell: bash
        run: |
          python -m venv venv
          if [ "${{ runner.os }}" == "Windows" ]; then
            source venv/Scripts/activate
            pip install PyQt5
          else
            source venv/bin/activate
          fi

          pip install aubio/dist/*.whl
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Compile Pythran Modules
        shell: bash
        env:
          PYTHONUTF8: 1
        run: |
          if [ "${{ runner.os }}" == "Windows" ]; then
            source venv/Scripts/activate
            PYTHRAN_EXT=".pyd";
          else
            source venv/bin/activate
            PYTHRAN_EXT=".so";
          fi

          for file in System/Accelerated/*.py; do
            if grep -q "pythran export" "$file"; then
              FILENAME=$(basename "$file" .py)
              DIR_NAME=$(dirname "$file")
              
              pythran "$file" -o "$DIR_NAME/$FILENAME$PYTHRAN_EXT"
            fi
          done

      - name: Cleanup unnecessary files
        shell: bash
        run: |
          if [ "${{ runner.os }}" == "Linux" ]; then
            rm System/ADB/adb-macos
            rm System/ADB/adb.exe
            rm System/ADB/AdbWinApi.dll
            rm System/ADB/AdbWinUsbApi.dll
            rm System/FFmpeg/ffmpeg-windows.exe
            rm System/FFmpeg/ffprobe-windows.exe
            rm System/FFmpeg/ffmpeg-macos-intel
            rm System/FFmpeg/ffmpeg-macos-silicon
            rm System/FFmpeg/ffprobe-macos-intel
            rm System/FFmpeg/ffprobe-macos-silicon
          
            elif [ "${{ runner.os }}" == "Windows" ]; then
            rm System/ADB/adb-macos
            rm System/ADB/adb-linux
            rm System/FFmpeg/ffmpeg-macos-intel
            rm System/FFmpeg/ffmpeg-macos-silicon
            rm System/FFmpeg/ffprobe-macos-intel
            rm System/FFmpeg/ffprobe-macos-silicon
            rm System/FFmpeg/ffmpeg-linux
            rm System/FFmpeg/ffprobe-linux
          
            elif [ "${{ runner.os }}" == "macOS" ]; then
            rm System/ADB/adb-linux
            rm System/ADB/adb.exe
            rm System/ADB/AdbWinApi.dll
            rm System/ADB/AdbWinUsbApi.dll
            rm System/FFmpeg/ffmpeg-linux
            rm System/FFmpeg/ffprobe-linux
            rm System/FFmpeg/ffmpeg-windows.exe
            rm System/FFmpeg/ffprobe-windows.exe
          fi

          rm System/ADB/CassetteReceiver.apk

      - name: Run PyInstaller
        shell: bash
        run: |
          if [ "${{ runner.os }}" == "Windows" ]; then
            source venv/Scripts/activate
          else
            source venv/bin/activate
          fi

          pyinstaller --noconfirm cassette.spec

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: cassette-${{ matrix.artifact_name }}
          path: |
            ${{ runner.os == 'macOS' && 'dist/' || 'dist/Cassette' }}
            ${{ runner.os == 'macOS' && '!dist/Cassette' || '' }}
          include-hidden-files: true